#include "cliente.h"
#include "relatorio.h"
#include "fila.h"
#include <stdlib.h>
#include <stdio.h>



/*void read(){
	int m,n;
	scanf("%d %d", &m, &n);			//Lê valores de m e n

	for (int i = 0; i < n; ++i)
	{
		int o,v;
		cliente x;
		scanf("%d %d %d",&x.c,&o,&v);
		push(x);
		if (filaCheia())
		{
			atende();
		}
	}
	return;
}
*/


int main() {
	//read();				//Função de leitura da entrada
	//imprime();
	
	return 0;
}
